# Reproduction log

Copy this file per reproduction attempt and fill in every field.

## Environment

- Date:
- Operator:
- Git commit: `git rev-parse HEAD`
- Python version: `python --version`
- Package versions: `pip freeze | grep -E "torch|numpy|scipy|scikit-image|matplotlib|pandas|PyYAML"`
- OS / hardware:

## Run

- Config file:
- Config snapshot checked against repo config (yes/no):
- Command(s) executed:

```bash

```

- Seed:
- Wall-clock time:

## Outputs

- Run directory:
- `metrics/summary.csv` present (yes/no):
- Key numbers (mask, method, psnr_mean, ssim_mean, nrmse_mean):

| mask | method | psnr_mean | ssim_mean | nrmse_mean |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## Comparison against reference

- Reference run / commit:
- Metrics match within tolerance (state tolerance):
- Deviations and suspected causes:

## Notes

-
